[user]
	email = sathors@free.fr
	name = Sathors
[alias]
	st = status
	ci = commit
	d = diff
	dc = diff --cached
	co = checkout
	br = branch
	lf = log --graph --all --full-history --format='%C(yellow) %h %Cblue %an %C(white) %C(auto) %d %s'
